rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read access to all demo files
    match /demos/{allPaths=**} {
      allow read: if true;
      allow write: if true;  // Allow uploads for now - restrict later with auth
    }
  }
}
