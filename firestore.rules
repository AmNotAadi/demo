rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write demos (for now - no auth)
    match /demos/{demoId} {
      allow read: true;
      allow write: true;
      allow delete: true;
    }
  }
}
